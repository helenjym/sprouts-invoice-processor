before:
- trying to implement functionality wo a backend server

wednesday dec 24
----
- refactored endpoints so that when a user calls upload, backend not only converts in into an invoice payment data structure but makes the
  IPR and file, persists to disk, to be returned to the user, so that when donload/invoicenumber is called, a file is just being returned
    - my logic for doing this: so backend validation can be solely done in the upload endpoint rather than having to do it across both /upload and /donload...idk tbh
    - invoice number param in the endpoint is checked against a parallel json file that is made when upload last called
- added validation in the frontend
- added header that changes depending on the time YIPPEEEEE

notes for tomorrow:
- validate file to ensure it is a pdf and not some other file for security

friday dec 26
----
- refactored POST endpoint to use async/await instead of then because of a bug of file n to disc after needing it ?!
- implemented new form component that connects to Horizon invoice parsing functionality through a new api endpoint 
  /upload/Horizon, extensions to other invoices I implement parsing for can be done through
  this component
  - still have to validate inputs for backend

notes for tomorrow
- fix form for manual invoice submission form
- fix component structure
- finally start on UI

tuesday dec 30
----
- implemented storing of known supplier info in the backend (currently just name and email)
  frontend makes request to backend for this info on page load (doesn't necessarily have to make this call all the time? idk)
  to display in supplier dropdown
  - why? so known supplier email is automatically filled out and
    so known suppliers (suppliers for which the AMS has EFT info for) can be
    updated and managed in the backend wo the frontend having to care
  - currently using useEffect to get data, is this a good method?


saturday, jan 3
----
- finished adding server side validation for /upload endpoint
  - checked text inputs for valid characters and non-empty
  - checked file for being under 5 MB and having the magic signature
    of a pdf
      - the thing is, i only validate after it gets uploaded to disk...is that secure?
      - using express-validator to help with non-file inptu validation, it returns an
        errors array which is helpful to give info to client..but this may not be a necessary dependency?
  
questions:
- can i do this all without a server !?!?!
- pdf-parse works on the web but react?
- what are the dangers of doing uploading/downloading over the internet
- HOW does React work exactly ?!?!!??!?

tomorrow:
- frontend validation
- style Horizon form + do backend validation
- draw background for evening hehe

monday:
- extend parsing to other suppliers

monday, jan 5
- changed invoiceNum to string
- validated file received from /download/:iv endpoint
  - checked file for being under 5 MB and having the magic signature
    of a pdf
- download button component is conditional on whether user has
  ever submitted the form succesfully before (on page open, it will not download anything if clicked)

